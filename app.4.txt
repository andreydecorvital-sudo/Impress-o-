async function run(){const raw=z.value.trim();if(!raw){msg('Cole um ZPL antes de continuar.','err');return}go.disabled=true;go.textContent='Processando...';out.innerHTML='<div class="empty">Abrindo as páginas...</div>';progress(2);try{await loadLibraries();const items=decodeImages(raw);if(!items.length)throw new Error('Nenhum gráfico GRF/Z64 foi encontrado');for(let i=0;i<items.length;i++)await ocr(items[i],i,items.length);msg('Lendo descrição e calculando quantidade...','warn');progress(72);const pairs=pairPages(items);if(!pairs.length)throw new Error('Não encontrei etiqueta de envio para processar');out.innerHTML='';const zpls=[];let noDetail=0;for(let i=0;i<pairs.length;i++){const pair=pairs[i];if(!pair.detail){noDetail++;continue}const label=packingLabel(pair.detail.text);const result=fuse(pair.ship.canvas,label);zpls.push(canvasToGfa(result.canvas));showCard(pair,result,i)}if(!zpls.length)throw new Error('Não encontrei descrição de itens para combinar com as etiquetas');finalZpl=zpls.join(NL+NL);cp.disabled=dl.disabled=false;progress(100);const summary=document.createElement('div');summary.className='summary';summary.innerHTML='<b>'+zpls.length+' etiqueta(s) gerada(s).</b><br>A descrição foi resumida e a quantidade total foi escrita em letras grandes diretamente na etiqueta de envio.'+(noDetail?'<br><b>'+noDetail+' etiqueta(s) ficaram sem descrição pareada.</b>':'');out.prepend(summary);msg(zpls.length+' etiqueta(s) prontas. Confira a frase grande na prévia.','ok');out.scrollIntoView({behavior:'smooth',block:'start'})}catch(e){console.error(e);out.innerHTML='<div class="empty"><b>Erro:</b><br>'+esc(e.message)+'</div>';msg('Erro: '+e.message,'err');progress(0)}finally{go.disabled=false;go.textContent='Gerar etiquetas'}}
go.addEventListener('click',run);q('#clear').addEventListener('click',()=>{z.value='';finalZpl='';out.innerHTML='<div class="empty">Nenhuma etiqueta processada.</div>';cp.disabled=dl.disabled=true;msg('Pronto.','ok');progress(0)});cp.addEventListener('click',async()=>{try{await navigator.clipboard.writeText(finalZpl);msg('ZPL copiado.','ok')}catch(e){msg('Use o botão de download.','err')}});dl.addEventListener('click',()=>{const blob=new Blob([finalZpl],{type:'text/plain;charset=utf-8'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='etiquetas-item-grande.txt';a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)});
})();